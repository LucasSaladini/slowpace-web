services:
  slowpace-api:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    container_name: slowpace-api
    restart: always
    ports:
      - "3333:3333"
    environment:
      - DATABASE_URL
      - JWT_SECRET
      - COOKIE_SECRET
      - FRONTEND_URL
    networks:
      - slowpace-network

networks:
  slowpace-network:
    external: true