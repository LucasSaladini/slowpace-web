services:
  slowpace-api:
    build:
      context: .
      dockerfile: slowpace-api
    container_name: slowpace-api
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - slowpace-network

networks:
  slowpace-network:
    external: true